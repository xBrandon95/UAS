{
  "name": "monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "license": "ISC",
  "packageManager": "pnpm@10.28.0",
  "devDependencies": {
    "turbo": "^2.7.5"
  },
  "engines": {
    "node": ">=18"
  }
}
